"""Main GUI application (Tkinter).

Planned contents (implemented in the GUI step of the plan):

- Step-by-step animation of the agent moving through the maze.
- Controls: algorithm selection, source/target environment selection,
  train/evaluate mode, start / pause / resume / reset / rerun, animation
  speed, policy-overlay toggle.
- Live status panel: episode number, step count, cumulative reward, epsilon,
  key status, gate phase and recent success rate.
- Instant visual feedback for key pickup, door opening, wall hits,
  penalty-cell entry and episode success/failure.
"""
