"""Maze renderer used by the GUI.

Planned contents (implemented in the GUI step of the plan):

- Distinct, polished visuals for every cell type: walls, normal cells,
  penalty cells, start, key, locked/open door, goal, the periodic gate
  (visibly open vs. closed) and the agent itself.
- Optional overlays: value heatmap, policy arrows, visit counts and the
  agent's trajectory.
"""
